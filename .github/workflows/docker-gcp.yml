name: Build and Push to Artifact Registry

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - id: 'auth'
        name: Google Auth
        uses: 'google-github-actions/auth@v2'
        with:
          workload_identity_provider: 'projects/${{ secrets.GCP_PROJECT_NUMBER }}/locations/global/workloadIdentityPools/${{ secrets.WIF_POOL_ID }}/providers/${{ secrets.WIF_PROVIDER_ID }}'
          service_account: '${{ secrets.GCP_SERVICE_ACCOUNT }}'

      - name: 'Set up Cloud SDK'
        uses: 'google-github-actions/setup-gcloud@v2'

      - name: 'Docker configuration'
        run: gcloud auth configure-docker europe-west1-docker.pkg.dev

      - name: Build Docker image
        run: |
          docker build -t ${{ secrets.GCP_ARTIFACT_REGISTRY }}/gym-app-backend:${{ github.sha }} -f Backend/Dockerfile Backend

      - name: Push Docker image with SHA tag
        run: |
          docker push ${{ secrets.GCP_ARTIFACT_REGISTRY }}/gym-app-backend:${{ github.sha }}

      - name: Tag image as 'latest'
        run: |
          docker tag ${{ secrets.GCP_ARTIFACT_REGISTRY }}/gym-app-backend:${{ github.sha }} ${{ secrets.GCP_ARTIFACT_REGISTRY }}/gym-app-backend:latest
      
      - name: Push Docker image with 'latest' tag
        run: |
          docker push ${{ secrets.GCP_ARTIFACT_REGISTRY }}/gym-app-backend:latest
